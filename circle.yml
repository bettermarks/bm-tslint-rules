machine:
  node:
    version: v8.9.4
  services:
    - docker
dependencies:
  override:
    - npm install
test:
  override:
    - npm test
  post:
    - ./ci-test-clean-build.sh
    - ./ci-test-master-version.sh

machine:
  node:
    version: v8.2.0
  services:
    - docker
dependencies:
  override:
    - npm install
test:
  override:
    - npm test
  post:
    - npm run build
#    after build there shouldn't be a diff, otherwise dist folder is not in sync
    - git diff --exit-code
